# Entornos y bytecode
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Secretos
.env

# Datos regenerables (no se versionan; se redescargan con yfinance)
data/

# Caché: los modelos y las decisiones del agente (cache/models, cache/agent) SÍ se
# versionan. Las respuestas LLM crudas no: cache/llm es grande y ruidoso, se conserva
# solo en disco (no se pierde, pero no entra en git).
cache/llm/

# Sistema operativo
.DS_Store
Icon?

# IDE / herramientas locales
.idea/
.vscode/
.claude/*
!.claude/agents/
!.claude/agents/**
!.claude/commands/
!.claude/commands/**
logs/

# Audios crudos del tutor (las transcripciones .md sí se versionan)
docs/tutor_audios/

# Tests y coverage
.pytest_cache/
.coverage
htmlcov/

# Caché de linters
.ruff_cache/

# Salidas intermedias de notebooks
.ipynb_checkpoints/

# Backups transitorios de calibración (el rastro está en BITACORA)
cache/models/*_legacy*.pkl
# Notebook scratch local
notebooks/logic_esential.ipynb

# Muestras de estilo de Raquel: PRIVADAS (repo público). Solo se versiona el README;
# los textos propios (TFB, ensayos…) se quedan en local para calibrar el estilo.
tesis_assets/estilo_raquel/*
!tesis_assets/estilo_raquel/README.md
